"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ServerAuthenticator = exports.AUTH_RDM_MONOTONIC_COUNTER = exports.AUTH_ALGORITHM_HMAC_MD5 = exports.AUTH_PROTOCOL_DELAYED = exports.AUTH_PROTOCOL_CONFIGURATION_TOKEN = exports.AUTHENTICATION_OPTION = void 0;
exports.parseAuthentication = parseAuthentication;
exports.encodeAuthentication = encodeAuthentication;
exports.createDelayedAuthenticationRequest = createDelayedAuthenticationRequest;
exports.authenticationMessageForMac = authenticationMessageForMac;
exports.computeDelayedAuthenticationMac = computeDelayedAuthenticationMac;
const node_crypto_1 = require("node:crypto");
const node_buffer_1 = require("node:buffer");
const errors_js_1 = require("./errors.js");
exports.AUTHENTICATION_OPTION = 90;
exports.AUTH_PROTOCOL_CONFIGURATION_TOKEN = 0;
exports.AUTH_PROTOCOL_DELAYED = 1;
exports.AUTH_ALGORITHM_HMAC_MD5 = 1;
exports.AUTH_RDM_MONOTONIC_COUNTER = 0;
function parseAuthentication(input) {
    const bytes = Uint8Array.from(input);
    if (bytes.length < 11) {
        throw new errors_js_1.ProtocolError('DHCP authentication option must be at least 11 bytes');
    }
    const protocol = bytes[0];
    const algorithm = bytes[1];
    const replayDetectionMethod = bytes[2];
    const replay = node_buffer_1.Buffer.from(bytes.subarray(3, 11)).readBigUInt64BE();
    const information = bytes.slice(11);
    if (replayDetectionMethod !== exports.AUTH_RDM_MONOTONIC_COUNTER) {
        throw new errors_js_1.ProtocolError(`Unsupported DHCP authentication replay method: ${replayDetectionMethod}`);
    }
    if (protocol === exports.AUTH_PROTOCOL_CONFIGURATION_TOKEN) {
        if (algorithm !== 0) {
            throw new errors_js_1.ProtocolError('Configuration-token authentication requires algorithm 0');
        }
        return Object.freeze({
            protocol,
            algorithm,
            replayDetectionMethod,
            replay,
            information,
            request: false,
        });
    }
    if (protocol === exports.AUTH_PROTOCOL_DELAYED) {
        if (algorithm !== exports.AUTH_ALGORITHM_HMAC_MD5) {
            throw new errors_js_1.ProtocolError(`Unsupported delayed authentication algorithm: ${algorithm}`);
        }
        if (information.length === 0) {
            return Object.freeze({
                protocol,
                algorithm,
                replayDetectionMethod,
                replay,
                information,
                request: true,
            });
        }
        if (information.length !== 20) {
            throw new errors_js_1.ProtocolError('Delayed authentication information must be empty or 20 bytes');
        }
        const secretId = node_buffer_1.Buffer.from(information.subarray(0, 4)).readUInt32BE();
        const mac = information.slice(4);
        return Object.freeze({
            protocol,
            algorithm,
            replayDetectionMethod,
            replay,
            information,
            secretId,
            mac,
            request: false,
        });
    }
    throw new errors_js_1.ProtocolError(`Unsupported DHCP authentication protocol: ${protocol}`);
}
function encodeAuthentication(authentication) {
    const header = node_buffer_1.Buffer.alloc(11);
    header[0] = authentication.protocol;
    header[1] = authentication.algorithm;
    header[2] = authentication.replayDetectionMethod;
    header.writeBigUInt64BE(authentication.replay, 3);
    return [...header, ...(authentication.information ?? [])];
}
function createDelayedAuthenticationRequest(replay) {
    return encodeAuthentication({
        protocol: exports.AUTH_PROTOCOL_DELAYED,
        algorithm: exports.AUTH_ALGORITHM_HMAC_MD5,
        replayDetectionMethod: exports.AUTH_RDM_MONOTONIC_COUNTER,
        replay,
    });
}
function authenticationMessageForMac(wire) {
    const input = node_buffer_1.Buffer.from(wire);
    if (input.length < 240) {
        throw new errors_js_1.ProtocolError('DHCP packet is too short for authentication');
    }
    const header = node_buffer_1.Buffer.from(input.subarray(0, 240));
    header[3] = 0;
    header.fill(0, 24, 28);
    const output = [header];
    for (let offset = 240; offset < input.length;) {
        const code = input[offset];
        if (code === 255) {
            output.push(node_buffer_1.Buffer.from(input.subarray(offset)));
            break;
        }
        if (code === 0) {
            output.push(node_buffer_1.Buffer.from([0]));
            offset += 1;
            continue;
        }
        if (offset + 1 >= input.length) {
            throw new errors_js_1.ProtocolError('Truncated DHCP option while authenticating');
        }
        const length = input[offset + 1];
        const end = offset + 2 + length;
        if (end > input.length) {
            throw new errors_js_1.ProtocolError('Truncated DHCP option while authenticating');
        }
        if (code !== 82) {
            const option = node_buffer_1.Buffer.from(input.subarray(offset, end));
            if (code === exports.AUTHENTICATION_OPTION && length === 31 && option[2] === exports.AUTH_PROTOCOL_DELAYED) {
                option.fill(0, option.length - 16);
            }
            output.push(option);
        }
        offset = end;
    }
    return node_buffer_1.Buffer.concat(output);
}
function computeDelayedAuthenticationMac(wire, secret) {
    return (0, node_crypto_1.createHmac)('md5', secret).update(authenticationMessageForMac(wire)).digest();
}
class ServerAuthenticator {
    #config;
    #contexts = new WeakMap();
    #lastReplay = new Map();
    #outgoingReplay = BigInt(Date.now()) << 16n;
    constructor(config) {
        this.#config = config;
        if (config.mode === 'token' && toBytes(config.token).length === 0) {
            throw new errors_js_1.ConfigurationError('DHCP authentication token must not be empty');
        }
    }
    authenticate(packet, wire) {
        const raw = packet.options[exports.AUTHENTICATION_OPTION];
        if (!isByteValue(raw)) {
            return this.#config.required === false
                ? { accepted: true }
                : {
                    accepted: false,
                    reason: 'Missing DHCP authentication option',
                };
        }
        let authentication;
        try {
            authentication = parseAuthentication(raw);
        }
        catch (error) {
            return {
                accepted: false,
                reason: error instanceof Error ? error.message : String(error),
            };
        }
        if (this.#config.mode === 'token') {
            return this.#authenticateToken(packet, authentication);
        }
        return this.#authenticateDelayed(packet, wire, authentication);
    }
    prepareResponse(response, request) {
        const context = this.#contexts.get(request);
        if (!context) {
            return;
        }
        this.#contexts.set(response, context);
        const replay = this.#nextReplay();
        if (context.mode === 'token') {
            const token = toBytes(this.#config.mode === 'token' ? this.#config.token : '');
            response.options[exports.AUTHENTICATION_OPTION] = [
                ...encodeAuthentication({
                    protocol: 0,
                    algorithm: 0,
                    replayDetectionMethod: 0,
                    replay,
                }),
                ...token,
            ];
            return;
        }
        const key = context.key;
        const information = node_buffer_1.Buffer.alloc(20);
        information.writeUInt32BE(key.secretId, 0);
        response.options[exports.AUTHENTICATION_OPTION] = encodeAuthentication({
            protocol: 1,
            algorithm: 1,
            replayDetectionMethod: 0,
            replay,
            information,
        });
    }
    signResponse(packet, formatPacket) {
        const context = this.#contexts.get(packet);
        const first = formatPacket();
        if (context?.mode !== 'delayed') {
            return first;
        }
        const authentication = parseAuthentication(packet.options[exports.AUTHENTICATION_OPTION]);
        const key = context.key;
        const information = node_buffer_1.Buffer.alloc(20);
        information.writeUInt32BE(key.secretId, 0);
        node_buffer_1.Buffer.from(computeDelayedAuthenticationMac(first, key.secret)).copy(information, 4);
        packet.options[exports.AUTHENTICATION_OPTION] = encodeAuthentication({
            protocol: 1,
            algorithm: 1,
            replayDetectionMethod: 0,
            replay: authentication.replay,
            information,
        });
        return formatPacket();
    }
    #authenticateToken(packet, authentication) {
        if (authentication.protocol !== 0) {
            return {
                accepted: false,
                reason: 'Expected configuration-token authentication',
            };
        }
        const expected = toBytes(this.#config.mode === 'token' ? this.#config.token : '');
        const actual = node_buffer_1.Buffer.from(authentication.information);
        if (actual.length !== expected.length || !(0, node_crypto_1.timingSafeEqual)(actual, expected)) {
            return {
                accepted: false,
                reason: 'Invalid DHCP authentication token',
            };
        }
        if (!this.#acceptReplay(`token:${clientIdentity(packet)}`, authentication.replay)) {
            return {
                accepted: false,
                reason: 'Replayed DHCP authentication token',
            };
        }
        this.#contexts.set(packet, { mode: 'token' });
        return {
            accepted: true,
            authentication,
        };
    }
    #authenticateDelayed(packet, wire, authentication) {
        if (authentication.protocol !== 1) {
            return {
                accepted: false,
                reason: 'Expected delayed DHCP authentication',
            };
        }
        const config = this.#config.mode === 'delayed' ? this.#config : undefined;
        if (authentication.request) {
            const key = config?.getKey(packet);
            if (!key) {
                return {
                    accepted: false,
                    reason: 'No DHCP authentication key for client',
                };
            }
            this.#contexts.set(packet, {
                mode: 'delayed',
                key,
            });
            return {
                accepted: true,
                authentication,
            };
        }
        const key = config?.getKey(packet, authentication.secretId);
        if (!key || key.secretId !== authentication.secretId) {
            return {
                accepted: false,
                reason: 'Unknown DHCP authentication secret ID',
            };
        }
        const expected = node_buffer_1.Buffer.from(computeDelayedAuthenticationMac(wire, key.secret));
        const actual = node_buffer_1.Buffer.from(authentication.mac);
        if (!(0, node_crypto_1.timingSafeEqual)(actual, expected)) {
            return {
                accepted: false,
                reason: 'Invalid DHCP authentication MAC',
            };
        }
        if (!this.#acceptReplay(`delayed:${clientIdentity(packet)}:${key.secretId}`, authentication.replay)) {
            return {
                accepted: false,
                reason: 'Replayed DHCP authenticated message',
            };
        }
        this.#contexts.set(packet, {
            mode: 'delayed',
            key,
        });
        return {
            accepted: true,
            authentication,
        };
    }
    #acceptReplay(key, replay) {
        const previous = this.#lastReplay.get(key);
        if (previous !== undefined && replay <= previous) {
            return false;
        }
        this.#lastReplay.set(key, replay);
        return true;
    }
    #nextReplay() {
        this.#outgoingReplay += 1n;
        return this.#outgoingReplay;
    }
}
exports.ServerAuthenticator = ServerAuthenticator;
function clientIdentity(packet) {
    const clientId = packet.options[61];
    return typeof clientId === 'string' ? clientId : packet.chaddr;
}
function isByteValue(value) {
    return value instanceof Uint8Array || (Array.isArray(value) && value.every((item) => Number.isInteger(item) && item >= 0 && item <= 255));
}
function toBytes(value) {
    return typeof value === 'string' ? node_buffer_1.Buffer.from(value, 'utf8') : node_buffer_1.Buffer.from(value);
}
//# sourceMappingURL=authentication.js.map