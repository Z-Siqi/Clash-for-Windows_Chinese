"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.protocol = exports.DHCP_FIXED_HEADER_LENGTH = exports.DHCP_MAGIC_COOKIE = void 0;
exports.parse = parse;
exports.format = format;
exports.formatBuffer = formatBuffer;
const node_buffer_1 = require("node:buffer");
const constants_js_1 = require("./constants.js");
const options_js_1 = require("./options.js");
const sequential_buffer_js_1 = require("./sequential-buffer.js");
exports.DHCP_MAGIC_COOKIE = 0x63825363;
exports.DHCP_FIXED_HEADER_LENGTH = 240;
function parse(input, registry = options_js_1.options) {
    const buffer = node_buffer_1.Buffer.isBuffer(input) ? input : node_buffer_1.Buffer.from(input);
    if (buffer.length < exports.DHCP_FIXED_HEADER_LENGTH) {
        throw new RangeError('Received DHCP packet is too short');
    }
    const reader = new sequential_buffer_js_1.SequentialBuffer(buffer);
    const op = reader.getUInt8();
    const htype = reader.getUInt8();
    const hlen = reader.getUInt8();
    const hops = reader.getUInt8();
    const xid = reader.getUInt32();
    const secs = reader.getUInt16();
    const flags = reader.getUInt16();
    const ciaddr = reader.getIP();
    const yiaddr = reader.getIP();
    const siaddr = reader.getIP();
    const giaddr = reader.getIP();
    const chaddr = reader.getMAC(htype, hlen);
    const sname = reader.getUTF8(64);
    const file = reader.getASCII(128);
    const magicCookie = reader.getUInt32();
    if (magicCookie !== exports.DHCP_MAGIC_COOKIE) {
        throw new TypeError('Invalid DHCP magic cookie');
    }
    const packetOptions = reader.getOptions(registry);
    return {
        op,
        htype,
        hlen,
        hops,
        xid,
        secs,
        flags,
        ciaddr,
        yiaddr,
        siaddr,
        giaddr,
        chaddr,
        sname,
        file,
        magicCookie,
        options: packetOptions,
    };
}
function format(packet, maximumSize = constants_js_1.DEFAULT_DHCP_MESSAGE_SIZE, registry = options_js_1.options) {
    const writer = new sequential_buffer_js_1.SequentialBuffer(null, maximumSize);
    writer.addUInt8(packet.op);
    writer.addUInt8(packet.htype);
    writer.addUInt8(packet.hlen);
    writer.addUInt8(packet.hops);
    writer.addUInt32(packet.xid);
    writer.addUInt16(packet.secs);
    writer.addUInt16(packet.flags);
    writer.addIP(packet.ciaddr);
    writer.addIP(packet.yiaddr);
    writer.addIP(packet.siaddr);
    writer.addIP(packet.giaddr);
    writer.addMac(packet.chaddr);
    writer.addUTF8(packet.sname, 64);
    writer.addASCII(packet.file, 128);
    writer.addUInt32(exports.DHCP_MAGIC_COOKIE);
    writer.addOptions(packet.options, registry);
    writer.addUInt8(255);
    return writer.seal();
}
function formatBuffer(packet, maximumSize = constants_js_1.DEFAULT_DHCP_MESSAGE_SIZE, registry = options_js_1.options) {
    return format(packet, maximumSize, registry).toBuffer();
}
exports.protocol = {
    parse,
    format,
    formatBuffer,
};
exports.default = exports.protocol;
//# sourceMappingURL=protocol.js.map