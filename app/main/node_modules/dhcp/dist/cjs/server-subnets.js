"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ServerSubnetRegistry = void 0;
const errors_js_1 = require("./errors.js");
const lease_manager_js_1 = require("./lease-manager.js");
const relay_agent_js_1 = require("./relay-agent.js");
const tools_js_1 = require("./tools.js");
class ServerSubnetRegistry {
    #optionRegistry;
    #configOptions;
    #context;
    #runtimes;
    #packetSubnets = new WeakMap();
    constructor(config, optionRegistry, configOptions, context) {
        this.#optionRegistry = optionRegistry;
        this.#configOptions = configOptions;
        this.#context = context;
        const configuredSubnets = config.subnets?.length
            ? config.subnets
            : config.range
                ? [{
                        ...config,
                        range: config.range,
                    }]
                : [];
        if (configuredSubnets.length === 0) {
            throw new errors_js_1.ConfigurationError('A DHCP range or at least one subnet is required');
        }
        const ids = new Set();
        this.#runtimes = configuredSubnets.map((subnet, index) => {
            const id = subnet.id ?? (configuredSubnets.length === 1 ? 'default' : `subnet-${index + 1}`);
            if (ids.has(id)) {
                throw new errors_js_1.ConfigurationError(`Duplicate DHCP subnet id: ${id}`);
            }
            ids.add(id);
            const merged = {
                ...config,
                ...subnet,
                id,
                range: subnet.range,
            };
            delete merged.subnets;
            const staticValue = merged.static;
            const staticBindings = typeof staticValue === 'function' ? (merged.staticReservations ?? {}) : (staticValue ?? {});
            if (!staticBindings || typeof staticBindings !== 'object' || Array.isArray(staticBindings)) {
                throw new errors_js_1.ConfigurationError(`Static bindings for subnet ${id} must be an object`);
            }
            const staticResolver = typeof staticValue === 'function' ? staticValue.bind(context) : undefined;
            merged.static = Object.freeze({ ...staticBindings });
            if (merged.allocationPolicy !== undefined && merged.allocationPolicy !== 'dynamic' && merged.allocationPolicy !== 'static-only') {
                throw new errors_js_1.ConfigurationError(`Invalid allocationPolicy for subnet ${id}: ${String(merged.allocationPolicy)}`);
            }
            const serverAddress = String(this.resolveFromConfig(merged, 'server', undefined, context) ?? subnet.range[0]);
            const leases = new lease_manager_js_1.LeaseManager({
                range: subnet.range,
                staticBindings: merged.static,
                serverAddress,
                random: Boolean(this.resolveFromConfig(merged, 'randomIP', undefined, context) ?? true),
            });
            merged.relayReservations?.forEach((address, reservationIndex) => {
                leases.reserveStaticAddress(`relay:${id}:reservation:${reservationIndex}`, address);
            });
            if (Array.isArray(merged.relayBindings)) {
                merged.relayBindings.forEach((binding, bindingIndex) => {
                    if (!binding.circuitId && !binding.remoteId) {
                        throw new errors_js_1.ConfigurationError('A declarative Relay Agent binding requires a circuitId or remoteId');
                    }
                    if (binding.giaddr === '0.0.0.0') {
                        throw new errors_js_1.ConfigurationError('A declarative Relay Agent binding requires a non-zero giaddr');
                    }
                    (0, tools_js_1.parseIp)(binding.giaddr);
                    if (binding.circuitId) {
                        (0, relay_agent_js_1.relayAgentIdentifierBytes)(binding.circuitId);
                    }
                    if (binding.remoteId) {
                        (0, relay_agent_js_1.relayAgentIdentifierBytes)(binding.remoteId);
                    }
                    leases.reserveStaticAddress(`relay:${id}:binding:${bindingIndex}`, binding.address);
                });
            }
            return Object.freeze({
                id,
                config: Object.freeze(merged),
                ...(staticResolver ? { staticResolver } : {}),
                leases,
            });
        });
    }
    get size() {
        return this.#runtimes.length;
    }
    get defaultRuntime() {
        return this.#runtimes[0];
    }
    runtime(packet) {
        if (!packet) {
            if (this.#runtimes.length !== 1) {
                throw new errors_js_1.ConfigurationError('A packet is required to select one of multiple DHCP subnets');
            }
            return this.defaultRuntime;
        }
        const cached = this.#packetSubnets.get(packet);
        if (cached) {
            return cached;
        }
        const customMatches = this.#runtimes.filter(({ config }) => config.match?.(packet) === true);
        if (customMatches.length > 1) {
            throw new errors_js_1.ConfigurationError('Multiple DHCP subnet match callbacks accepted the packet');
        }
        if (customMatches.length === 1) {
            return this.associate(packet, customMatches[0]);
        }
        const candidates = [packet.options[118], packet.giaddr, packet.options[50], packet.ciaddr, packet.yiaddr]
            .filter((value) => typeof value === 'string' && value !== '0.0.0.0');
        for (const address of candidates) {
            const matches = this.#runtimes.filter((runtime) => this.contains(runtime, address, packet, this.#context));
            if (matches.length === 1) {
                return this.associate(packet, matches[0]);
            }
            if (matches.length > 1) {
                throw new errors_js_1.ConfigurationError(`Address ${address} matches multiple DHCP subnets`);
            }
        }
        if (this.#runtimes.length === 1) {
            return this.associate(packet, this.defaultRuntime);
        }
        throw new errors_js_1.ConfigurationError('Unable to select a DHCP subnet; configure giaddr, subnet selection, requested address, ciaddr, or a match callback');
    }
    associate(packet, runtime) {
        this.#packetSubnets.set(packet, runtime);
        return runtime;
    }
    resolve(key, packet, context) {
        return this.resolveFromConfig(this.runtime(packet).config, key, packet, context);
    }
    snapshot() {
        if (this.#runtimes.length === 1) {
            return this.defaultRuntime.leases.snapshot();
        }
        return Object.fromEntries(this.#runtimes.flatMap(({ id, leases }) => Object.entries(leases.snapshot()).map(([key, lease]) => [`${id}:${key}`, lease])));
    }
    combinedLeases() {
        return new Map(Object.entries(this.snapshot()));
    }
    byId(id) {
        return this.#runtimes.find((runtime) => runtime.id === id);
    }
    staticAddress(runtime, packet, clientKey) {
        const bindings = runtime.staticResolver?.(packet) ?? runtime.config.static ?? {};
        if (!bindings || typeof bindings !== 'object' || Array.isArray(bindings)) {
            throw new errors_js_1.ConfigurationError(`Static bindings for subnet ${runtime.id} must resolve to an object`);
        }
        runtime.leases.reserveStaticBindings(bindings);
        const normalized = (0, lease_manager_js_1.normalizeMac)(packet.chaddr);
        const macAddress = Object.entries(bindings).find(([mac]) => (0, lease_manager_js_1.normalizeMac)(mac) === normalized)?.[1];
        const relayAddress = this.relayAddress(runtime, packet);
        if (macAddress && relayAddress && macAddress !== relayAddress) {
            throw new errors_js_1.ConfigurationError(`MAC and Relay Agent bindings disagree for ${packet.chaddr}`);
        }
        if (relayAddress
            && !macAddress
            && typeof runtime.config.relayBindings === 'function'
            && !runtime.config.relayReservations?.includes(relayAddress)) {
            if (!clientKey) {
                throw new errors_js_1.ConfigurationError('A client key is required to reserve a Relay Agent binding');
            }
            runtime.leases.reserveStaticAddress(clientKey, relayAddress);
        }
        return macAddress ?? relayAddress;
    }
    relayAddress(runtime, packet) {
        const bindings = runtime.config.relayBindings;
        if (bindings === undefined) {
            return undefined;
        }
        const information = packet.relayAgentInformation;
        const context = {
            packet,
            subnetId: runtime.id,
            giaddr: packet.giaddr,
            ...(information?.circuitId ? { circuitId: information.circuitId } : {}),
            ...(information?.remoteId ? { remoteId: information.remoteId } : {}),
        };
        if (typeof bindings === 'function') {
            return bindings.call(this.#context, context);
        }
        const matches = bindings.filter((binding) => {
            if (!binding.circuitId && !binding.remoteId) {
                throw new errors_js_1.ConfigurationError('A declarative Relay Agent binding requires a circuitId or remoteId');
            }
            return binding.giaddr === packet.giaddr
                && (0, relay_agent_js_1.relayAgentIdentifierEquals)(information?.circuitId, binding.circuitId)
                && (0, relay_agent_js_1.relayAgentIdentifierEquals)(information?.remoteId, binding.remoteId);
        });
        if (matches.length > 1) {
            throw new errors_js_1.ConfigurationError(`Multiple Relay Agent bindings match subnet ${runtime.id}`);
        }
        return matches[0]?.address;
    }
    contains(runtime, address, packet, context) {
        const mask = String(this.resolveFromConfig(runtime.config, 'netmask', packet, context));
        const cidr = (0, tools_js_1.CIDRFromNetmask)(mask);
        const network = (0, tools_js_1.parseIp)(runtime.config.range[0]) & (cidr === 0 ? 0 : (0xffffffff << (32 - cidr)) >>> 0);
        return ((0, tools_js_1.parseIp)(address) & (cidr === 0 ? 0 : (0xffffffff << (32 - cidr)) >>> 0)) === network;
    }
    resolveFromConfig(config, key, packet, context) {
        let value = config[key];
        if (typeof value === 'function') {
            value = value.call(context, packet);
        }
        if (value !== undefined) {
            return value;
        }
        const range = config.range;
        switch (key) {
            case 'range':
                return range;
            case 'static':
                return {};
            case 'randomIP':
                return true;
            case 'server':
                return range[0];
            case 'netmask':
                return (0, tools_js_1.formatIp)((0, tools_js_1.netmaskFromRange)(range[0], range[1]));
            case 'router':
                return range[0];
            case 'broadcast':
                {
                    const netmask = String(this.resolveFromConfig(config, 'netmask', packet, context));
                    return (0, tools_js_1.formatIp)((0, tools_js_1.broadcastFromIpCIDR)(range[0], (0, tools_js_1.CIDRFromNetmask)(netmask)));
                }
            default:
                return this.#optionRegistry[this.#configOptions[key] ?? -1]?.default;
        }
    }
}
exports.ServerSubnetRegistry = ServerSubnetRegistry;
//# sourceMappingURL=server-subnets.js.map