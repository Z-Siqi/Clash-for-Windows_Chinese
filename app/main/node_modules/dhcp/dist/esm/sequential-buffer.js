import { DEFAULT_DHCP_MESSAGE_SIZE } from './constants.js';
import { options } from './options.js';
export class SequentialBuffer {
    data;
    readOffset = 0;
    writeOffset = 0;
    constructor(buffer, length = DEFAULT_DHCP_MESSAGE_SIZE) {
        this.data = buffer ?? Buffer.alloc(length);
    }
    get _data() {
        return this.data;
    }
    get _r() {
        return this.readOffset;
    }
    set _r(value) {
        this.readOffset = value;
    }
    get _w() {
        return this.writeOffset;
    }
    set _w(value) {
        this.writeOffset = value;
    }
    addUInt8(value) {
        this.ensureWrite(1);
        this.data.writeUInt8(value, this.advanceWrite(1));
    }
    getUInt8() {
        this.ensureRead(1);
        return this.data.readUInt8(this.advanceRead(1));
    }
    addInt8(value) {
        this.ensureWrite(1);
        this.data.writeInt8(value, this.advanceWrite(1));
    }
    getInt8() {
        this.ensureRead(1);
        return this.data.readInt8(this.advanceRead(1));
    }
    addUInt16(value) {
        this.ensureWrite(2);
        this.data.writeUInt16BE(value, this.advanceWrite(2));
    }
    getUInt16() {
        this.ensureRead(2);
        return this.data.readUInt16BE(this.advanceRead(2));
    }
    addInt16(value) {
        this.ensureWrite(2);
        this.data.writeInt16BE(value, this.advanceWrite(2));
    }
    getInt16() {
        this.ensureRead(2);
        return this.data.readInt16BE(this.advanceRead(2));
    }
    addUInt32(value) {
        this.ensureWrite(4);
        this.data.writeUInt32BE(value >>> 0, this.advanceWrite(4));
    }
    getUInt32() {
        this.ensureRead(4);
        return this.data.readUInt32BE(this.advanceRead(4));
    }
    addInt32(value) {
        this.ensureWrite(4);
        this.data.writeInt32BE(value, this.advanceWrite(4));
    }
    getInt32() {
        this.ensureRead(4);
        return this.data.readInt32BE(this.advanceRead(4));
    }
    addUTF8(value, fixedLength) {
        this.addString(value, 'utf8', fixedLength);
    }
    getUTF8(length) {
        return this.getString(length, 'utf8');
    }
    addASCII(value, fixedLength) {
        this.addString(value, 'ascii', fixedLength);
    }
    getASCII(length) {
        return this.getString(length, 'ascii');
    }
    addIP(address) {
        const octets = address.split('.');
        if (octets.length !== 4) {
            throw new TypeError(`Invalid IPv4 address: ${address}`);
        }
        for (const octet of octets) {
            if (!/^\d{1,3}$/.test(octet) || Number(octet) > 255) {
                throw new TypeError(`Invalid IPv4 address: ${address}`);
            }
            this.addUInt8(Number(octet));
        }
    }
    getIP() {
        return [this.getUInt8(), this.getUInt8(), this.getUInt8(), this.getUInt8()].join('.');
    }
    addIPs(addresses) {
        for (const address of typeof addresses === 'string' ? [addresses] : addresses) {
            this.addIP(address);
        }
    }
    getIPs(length) {
        if (length % 4 !== 0) {
            throw new RangeError('An IP list option must be divisible by four bytes');
        }
        return Array.from({ length: length / 4 }, () => this.getIP());
    }
    addMac(address) {
        const octets = address.split(/[:-]/);
        if (octets.length !== 6) {
            throw new TypeError(`Invalid MAC address: ${address}`);
        }
        for (const octet of octets) {
            if (!/^[0-9a-f]{1,2}$/i.test(octet)) {
                throw new TypeError(`Invalid MAC address: ${address}`);
            }
            this.addUInt8(Number.parseInt(octet, 16));
        }
        this.ensureWrite(10);
        this.data.fill(0, this.writeOffset, this.writeOffset + 10);
        this.writeOffset += 10;
    }
    getMAC(hardwareType, hardwareLength) {
        if (hardwareType !== 1 || hardwareLength !== 6) {
            throw new TypeError(`Unsupported hardware address (length=${hardwareLength}, type=${hardwareType})`);
        }
        this.ensureRead(16);
        const result = [...this.data.subarray(this.readOffset, this.readOffset + 6)]
            .map((value) => value.toString(16).padStart(2, '0').toUpperCase())
            .join('-');
        this.readOffset += 16;
        return result;
    }
    addBool() { }
    getBool() {
        return true;
    }
    addUInt8s(values) {
        for (const value of typeof values === 'number' ? [values] : values) {
            this.addUInt8(value);
        }
    }
    getUInt8s(length) {
        return Array.from({ length }, () => this.getUInt8());
    }
    addUInt16s(values) {
        for (const value of typeof values === 'number' ? [values] : values) {
            this.addUInt16(value);
        }
    }
    getUInt16s(length) {
        if (length % 2 !== 0) {
            throw new RangeError('A UInt16 list option must have an even byte length');
        }
        return Array.from({ length: length / 2 }, () => this.getUInt16());
    }
    addOptions(values, registry = options) {
        for (const [rawId, originalValue] of Object.entries(values).sort(([left], [right]) => compareOptionCodes(Number(left), Number(right)))) {
            const id = Number(rawId);
            if (!Number.isInteger(id) || id < 1 || id > 254) {
                throw new RangeError(`Invalid DHCP option code: ${rawId}`);
            }
            const definition = registry[id];
            const encoded = definition?.type
                ? this.encodeOption(definition.type, originalValue)
                : this.encodeUnknownOption(id, originalValue);
            if (encoded === null) {
                continue;
            }
            if (encoded.length > 255) {
                throw new RangeError(`DHCP option ${id} exceeds 255 bytes`);
            }
            if (definition) {
                validateOption(id, encoded, definition);
            }
            this.addUInt8(id);
            this.addUInt8(encoded.length);
            this.ensureWrite(encoded.length);
            encoded.copy(this.data, this.writeOffset);
            this.writeOffset += encoded.length;
        }
    }
    getOptions(registry = options) {
        const encodedOptions = new Map();
        while (this.readOffset < this.data.length) {
            const id = this.getUInt8();
            if (id === 255) {
                break;
            }
            if (id === 0) {
                continue;
            }
            const length = this.getUInt8();
            this.ensureRead(length);
            const value = Buffer.from(this.data.subarray(this.readOffset, this.readOffset + length));
            this.readOffset += length;
            const chunks = encodedOptions.get(id) ?? [];
            chunks.push(value);
            encodedOptions.set(id, chunks);
        }
        const result = {};
        for (const [id, chunks] of encodedOptions) {
            const value = Buffer.concat(chunks);
            const definition = registry[id];
            if (!definition?.type) {
                result[id] = Uint8Array.from(value);
                continue;
            }
            validateOption(id, value, definition);
            const reader = new SequentialBuffer(value);
            result[id] = reader.readOption(definition.type, value.length);
            if (reader.readOffset !== value.length) {
                throw new Error(`DHCP option ${id} has an invalid length`);
            }
        }
        return result;
    }
    getHex(length) {
        this.ensureRead(length);
        return this.data.toString('hex', this.advanceRead(length), this.readOffset);
    }
    toBuffer() {
        return this.data.subarray(0, this.writeOffset);
    }
    seal() {
        this.data = this.toBuffer();
        return this;
    }
    addString(value, encoding, fixedLength) {
        const encoded = Buffer.from(value, encoding);
        if (fixedLength === undefined) {
            this.ensureWrite(encoded.length);
            encoded.copy(this.data, this.writeOffset);
            this.writeOffset += encoded.length;
            return;
        }
        this.ensureWrite(fixedLength);
        this.data.fill(0, this.writeOffset, this.writeOffset + fixedLength);
        encoded.copy(this.data, this.writeOffset, 0, fixedLength);
        this.writeOffset += fixedLength;
    }
    getString(length, encoding) {
        this.ensureRead(length);
        const value = this.data.toString(encoding, this.advanceRead(length), this.readOffset);
        const zero = value.indexOf('\0');
        return zero === -1 ? value : value.slice(0, zero);
    }
    encodeOption(type, value) {
        if (type === 'Bool' && value !== true && value !== 1 && !/^true|1$/i.test(String(value))) {
            return null;
        }
        if ((type === 'ASCII' || type === 'UTF8') && String(value).length === 0) {
            return null;
        }
        const buffer = new SequentialBuffer(null, 65535);
        switch (type) {
            case 'UInt8':
                buffer.addUInt8(Number(value));
                break;
            case 'Int8':
                buffer.addInt8(Number(value));
                break;
            case 'UInt16':
                buffer.addUInt16(Number(value));
                break;
            case 'Int16':
                buffer.addInt16(Number(value));
                break;
            case 'UInt32':
                buffer.addUInt32(Number(value));
                break;
            case 'Int32':
                buffer.addInt32(Number(value));
                break;
            case 'IP':
                buffer.addIP(String(value));
                break;
            case 'IPs':
                buffer.addIPs(value);
                break;
            case 'ASCII':
                buffer.addASCII(String(value));
                break;
            case 'UTF8':
                buffer.addUTF8(String(value));
                break;
            case 'Bool':
                buffer.addBool();
                break;
            case 'UInt8s':
                buffer.addUInt8s(value);
                break;
            case 'UInt16s':
                buffer.addUInt16s(value);
                break;
        }
        return buffer.toBuffer();
    }
    encodeUnknownOption(id, value) {
        if (!(value instanceof Uint8Array)) {
            throw new TypeError(`Unknown DHCP option ${id} must be encoded as Uint8Array or registered with createOptionRegistry()`);
        }
        return Buffer.from(value);
    }
    readOption(type, length) {
        switch (type) {
            case 'UInt8':
                if (length !== 1) {
                    break;
                }
                return this.getUInt8();
            case 'Int8':
                if (length !== 1) {
                    break;
                }
                return this.getInt8();
            case 'UInt16':
                if (length !== 2) {
                    break;
                }
                return this.getUInt16();
            case 'Int16':
                if (length !== 2) {
                    break;
                }
                return this.getInt16();
            case 'UInt32':
                if (length !== 4) {
                    break;
                }
                return this.getUInt32();
            case 'Int32':
                if (length !== 4) {
                    break;
                }
                return this.getInt32();
            case 'IP':
                if (length !== 4) {
                    break;
                }
                return this.getIP();
            case 'IPs':
                return this.getIPs(length);
            case 'ASCII':
                return this.getASCII(length);
            case 'UTF8':
                return this.getUTF8(length);
            case 'Bool':
                if (length !== 0) {
                    break;
                }
                return this.getBool();
            case 'UInt8s':
                return this.getUInt8s(length);
            case 'UInt16s':
                return this.getUInt16s(length);
        }
        throw new RangeError(`Invalid ${type} option length: ${length}`);
    }
    ensureRead(length) {
        if (length < 0 || this.readOffset + length > this.data.length) {
            throw new RangeError('Unexpected end of DHCP packet');
        }
    }
    ensureWrite(length) {
        if (length < 0 || this.writeOffset + length > this.data.length) {
            throw new RangeError('DHCP packet exceeds its maximum size');
        }
    }
    advanceRead(length) {
        const offset = this.readOffset;
        this.readOffset += length;
        return offset;
    }
    advanceWrite(length) {
        const offset = this.writeOffset;
        this.writeOffset += length;
        return offset;
    }
}
function compareOptionCodes(left, right) {
    if (left === 82) {
        return right === 82 ? 0 : 1;
    }
    if (right === 82) {
        return -1;
    }
    return left - right;
}
function validateOption(id, value, definition) {
    const length = value.length;
    const constraint = definition.length;
    if (typeof constraint === 'number') {
        if (length !== constraint) {
            throw new RangeError(`DHCP option ${id} must be exactly ${constraint} bytes`);
        }
    }
    else if (constraint?.min !== undefined && length < constraint.min) {
        throw new RangeError(`DHCP option ${id} must be at least ${constraint.min} bytes`);
    }
    if (typeof constraint === 'object' && constraint.max !== undefined && length > constraint.max) {
        throw new RangeError(`DHCP option ${id} must be at most ${constraint.max} bytes`);
    }
    if (typeof constraint === 'object' && constraint.multipleOf !== undefined && length % constraint.multipleOf !== 0) {
        throw new RangeError(`DHCP option ${id} length must be a multiple of ${constraint.multipleOf}`);
    }
    definition.validate?.(value);
}
export default SequentialBuffer;
//# sourceMappingURL=sequential-buffer.js.map