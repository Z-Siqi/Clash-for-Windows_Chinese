export class DhcpError extends Error {
    constructor(message, options) {
        super(message, options);
        this.name = new.target.name;
    }
}
export class PoolExhaustedError extends DhcpError {
    firstAddress;
    lastAddress;
    constructor(firstAddress, lastAddress) {
        super(`No DHCP addresses are available in ${firstAddress}-${lastAddress}`);
        this.firstAddress = firstAddress;
        this.lastAddress = lastAddress;
    }
}
export class ConfigurationError extends DhcpError {
}
export class ProtocolError extends DhcpError {
}
//# sourceMappingURL=errors.js.map