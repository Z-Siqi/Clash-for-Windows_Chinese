import dgram, { type Socket } from 'node:dgram';
import { EventEmitter } from 'node:events';
import type { ClientConfig, DhcpPacket, Lease, ListenOptions, Logger } from './types.js';
export declare class Client extends EventEmitter {
    #private;
    readonly logger: Logger;
    constructor(config?: ClientConfig, socket?: dgram.Socket);
    get _sock(): Socket;
    get _conf(): ClientConfig;
    get _state(): Lease;
    get state(): Readonly<Lease>;
    config(key: 'mac'): string;
    config(key: 'features'): readonly number[];
    sendDiscover(): void;
    handleOffer(response: DhcpPacket): void;
    sendRequest(offer: DhcpPacket): void;
    handleAck(response: DhcpPacket): void;
    sendRelease(_request?: DhcpPacket): void;
    sendRenew(): void;
    sendRebind(): void;
    handleForceRenew(response: DhcpPacket): void;
    listen(port?: number, host?: string, callback?: () => void): void;
    listen(options?: ListenOptions, callback?: () => void): void;
    close(callback?: () => void): void;
    _send(host: string, packet: DhcpPacket): void;
    private handleBuffer;
    private createRequest;
    private identityOptions;
    private maximumMessageSize;
    private matchesClientIdentifier;
    private emitState;
    private mapOptions;
    private addDerivedOptions;
    private scheduleLeaseTimers;
    private schedule;
    private clearTimers;
    private reportError;
}
