import type { DhcpPacket } from './types.js';
export type AddressProbeResult = 'available' | 'in-use' | 'unknown';
export interface AddressProbeContext {
    readonly packet: DhcpPacket;
    readonly subnetId: string;
    readonly timeoutMs: number;
}
export interface AddressProbe {
    probe(address: string, context: AddressProbeContext): AddressProbeResult | Promise<AddressProbeResult>;
}
export declare class SystemArpProbe implements AddressProbe {
    probe(address: string, context: AddressProbeContext): Promise<AddressProbeResult>;
}
export declare function interpretNeighborOutput(platform: NodeJS.Platform, output: string): AddressProbeResult;
