import type { IPv4Address, Lease, MacAddress } from './types.js';
export interface LeaseManagerOptions {
    range: readonly [IPv4Address, IPv4Address];
    staticBindings?: Readonly<Record<MacAddress, IPv4Address>>;
    serverAddress?: IPv4Address;
    random?: boolean;
    randomSource?: () => number;
    now?: () => Date;
}
export declare function normalizeMac(address: MacAddress): MacAddress;
export declare function createLease(): Lease;
export declare class LeaseManager {
    #private;
    constructor(options: LeaseManagerOptions);
    get leases(): ReadonlyMap<MacAddress, Lease>;
    snapshot(): Record<MacAddress, Lease>;
    get(macAddress: MacAddress): Lease | undefined;
    getOrCreate(macAddress: MacAddress): Lease;
    reserveStaticBindings(bindings: Readonly<Record<MacAddress, IPv4Address>>): void;
    reserveStaticAddress(clientKey: string, address: IPv4Address): void;
    quarantine(address: IPv4Address, seconds: number): void;
    select(clientKey: string, requestedAddress?: IPv4Address, hardwareAddress?: string, staticAddress?: IPv4Address): IPv4Address;
    offer(macAddress: MacAddress, address: IPv4Address, leasePeriod: number, server: IPv4Address): Lease;
    bind(macAddress: MacAddress, address: IPv4Address, leasePeriod: number, server: IPv4Address): Lease;
    release(macAddress: MacAddress): Lease | undefined;
    decline(macAddress: MacAddress, address?: IPv4Address): Lease;
    removeExpired(): number;
    private unavailableAddresses;
    private isInRange;
}
