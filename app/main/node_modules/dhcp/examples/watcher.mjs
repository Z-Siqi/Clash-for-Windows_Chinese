import { createBroadcastHandler, DHCPDISCOVER } from 'dhcp';

const watcher = createBroadcastHandler({ logLevel: 'warn' });

watcher.on('message', (packet) => {

    if (packet.options[53] === DHCPDISCOVER) {
        console.log(`Discovered ${packet.chaddr}`);
    }
});
watcher.listen();
